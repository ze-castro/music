# openssl rand -base64 32
ENCRYPTION_KEY=
# openssl rand -base64 48
SESSION_SECRET=
# Navidrome address as seen from inside Docker. Empty = users type it at login.
NAVIDROME_URL=http://192.168.1.50:4533
# Exact URL people open in the browser. Wrong value = login silently fails.
ORIGIN=https://music.example.com